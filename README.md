# IA_agentes-AgentProduct_ToolMcp
Agente realizado con el framework Google ADK que se conecta con dos herramientas (tools), una tool local que devuelve los precios de los productos y otra tool externa que devuelve el listado de los productos (que se consume a través del protocolo MCP).
